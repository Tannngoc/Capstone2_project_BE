name: Fetch Stock Data Daily

on:
  push:
    branches:
      - feature/train_AI
  schedule:
    - cron: '0 0 * * *'  # Chạy hàng ngày lúc 00:00 UTC
  workflow_dispatch:

concurrency:
  group: stock-data-update
  cancel-in-progress: true

jobs:
  fetch-data:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:latest
        ports:
          - 3306:3306
        env:
          MYSQL_ROOT_PASSWORD: tan12321
          MYSQL_DATABASE: starfall
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          ref: feature/train_AI

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install flask flask_sqlalchemy pymysql
          pip install yfinance pandas
          pip install tensorflow scikit-learn
        

      - name: Wait for MySQL to be ready
        run: |
          echo "⏳ Waiting for MySQL service to be ready..."
          for i in {1..20}; do
            if mysqladmin ping -h 127.0.0.1 -uroot -ptan12321 --silent; then
              echo "✅ MySQL is ready!"
              break
            fi
            echo "Waiting... ($i)"
            sleep 3
          done

      - name: Remove untracked files (if any)
        run: |
          git clean -fd
          echo "✅ Removed untracked files"

      - name: Reset local changes
        run: |
          git reset --hard
          echo "✅ Reset local changes"

      - name: Run data fetch script
        run: python scripts/fetch_stock_data.py

      - name: Import CSV data into database (Upsert)
        env:
          DATABASE_URI: mysql+pymysql://root:tan12321@127.0.0.1:3306/starfall
        run: |
          echo "Using DATABASE_URI: $DATABASE_URI"
          python import_csv_data.py
          echo "✅ Imported CSV data into database"

      - name: Commit and push new data
        run: |
          git config --global user.name "Tannngoc"
          git config --global user.email "github-actions@github.com"

          git add app/db/*.csv
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            DATE=$(date)
            git commit -m "📊 Auto-update stock data [$DATE]"
            git pull --rebase origin main
            git pull --rebase origin feature/train_AI
            git push origin feature/train_AI
          fi

      - name: Wait for 5 seconds (to ensure data is fully updated)
        run: |
          echo "⏳ Waiting for 5 seconds before training the model"
          sleep 5

      - name: Train AI Model
        run: |
          python app/AI/train_model.py
          echo "✅ Model trained and saved successfully"

      - name: Commit and push trained model
        run: |
          git config --global user.name "Tannngoc"
          git config --global user.email "github-actions@github.com"

          git add app/AI/joint_stock_model.keras
          if git diff --cached --quiet; then
            echo "⚠️ No model changes to commit"
          else
            DATE=$(date)
            git commit -m "🧠 Update trained AI model [$DATE]"
            git pull --rebase origin feature/train_AI
            git push origin feature/train_AI
            echo "✅ Model committed and pushed"
